apiVersion: v1
kind: Service
metadata:
  name: {{ include "socket.fullname" . }}
  labels:
    {{- include "socket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.websocket.port }}
      targetPort: websocket
      protocol: TCP
      name: websocket
  selector:
    {{- include "socket.selectorLabels" . | nindent 4 }}
