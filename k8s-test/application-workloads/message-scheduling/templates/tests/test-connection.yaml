apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "message-scheduling.fullname" . }}-test-connection"
  labels:
    {{- include "message-scheduling.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "message-scheduling.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
