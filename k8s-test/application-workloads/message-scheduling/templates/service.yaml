apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-scheduling.fullname" . }}
  labels:
    {{- include "message-scheduling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message-scheduling.selectorLabels" . | nindent 4 }}
