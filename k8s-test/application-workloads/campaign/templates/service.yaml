apiVersion: v1
kind: Service
metadata:
  name: {{ include "campaign.fullname" . }}
  labels:
    {{- include "campaign.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "campaign.selectorLabels" . | nindent 4 }}
