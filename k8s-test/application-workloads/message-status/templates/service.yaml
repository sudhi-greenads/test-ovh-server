apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-status.fullname" . }}
  labels:
    {{- include "message-status.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message-status.selectorLabels" . | nindent 4 }}
