apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rcs.fullname" . }}-test-connection"
  labels:
    {{- include "rcs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rcs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
