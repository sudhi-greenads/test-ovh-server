apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gupshup-inbound.fullname" . }}-test-connection"
  labels:
    {{- include "gupshup-inbound.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gupshup-inbound.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
