apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-creation.fullname" . }}
  labels:
    {{- include "message-creation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message-creation.selectorLabels" . | nindent 4 }}
