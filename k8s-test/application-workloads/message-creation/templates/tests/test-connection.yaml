apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "message-creation.fullname" . }}-test-connection"
  labels:
    {{- include "message-creation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "message-creation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
