apiVersion: v1
kind: Service
metadata:
  name: {{ include "campaign-management.fullname" . }}
  labels:
    {{- include "campaign-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "campaign-management.selectorLabels" . | nindent 4 }}
