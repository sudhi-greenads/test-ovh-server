apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "campaign-management.fullname" . }}-test-connection"
  labels:
    {{- include "campaign-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "campaign-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
