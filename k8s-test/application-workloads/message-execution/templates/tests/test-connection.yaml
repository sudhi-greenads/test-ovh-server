apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "message-execution.fullname" . }}-test-connection"
  labels:
    {{- include "message-execution.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "message-execution.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
