apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-execution.fullname" . }}
  labels:
    {{- include "message-execution.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message-execution.selectorLabels" . | nindent 4 }}
