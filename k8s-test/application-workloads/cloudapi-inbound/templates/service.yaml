apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudapi-inbound.fullname" . }}
  labels:
    {{- include "cloudapi-inbound.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudapi-inbound.selectorLabels" . | nindent 4 }}
