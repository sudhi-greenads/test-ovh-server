apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudapi-inbound.fullname" . }}-test-connection"
  labels:
    {{- include "cloudapi-inbound.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudapi-inbound.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
