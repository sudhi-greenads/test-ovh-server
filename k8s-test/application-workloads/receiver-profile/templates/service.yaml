apiVersion: v1
kind: Service
metadata:
  name: {{ include "receiver-profile.fullname" . }}
  labels:
    {{- include "receiver-profile.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "receiver-profile.selectorLabels" . | nindent 4 }}
