apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "receiver-profile.fullname" . }}-test-connection"
  labels:
    {{- include "receiver-profile.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "receiver-profile.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
